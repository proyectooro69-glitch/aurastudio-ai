import { createFileRoute } from '@tanstack/react-router'
import { AnimatePresence, motion } from 'framer-motion'
import { useRef, useState, type ReactNode } from 'react'
import { toast } from 'sonner'
import { Aperture, ArrowDownToLine, ChevronDown, Clapperboard, Clock3, Film, Image as ImageIcon, ImageUp, LayoutGrid, LoaderCircle, Menu, Moon, MoreHorizontal, Play, Plus, Settings2, Sparkles, Upload, WandSparkles, X } from 'lucide-react'

export const Route = createFileRoute('/')({
  head: () => ({ meta: [{ title: 'AuraStudio AI · Visual content, elevated' }, { name: 'description', content: 'Premium AI image and video creation workspace.' }, { name: 'theme-color', content: '#201d17' }] }),
  component: AuraStudio,
})

const imageUrl = 'https://images.unsplash.com/photo-1519608487953-e999c86e7455?auto=format&fit=crop&w=1800&q=90'
const secondImageUrl = 'https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=900&q=85'
const sampleVideo = 'https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4'
const presets = ['Cinematográfico', 'Lujo Comercial', 'Minimalista', 'Fotorrealista 8K']
const aspectRatios = ['9:16', '1:1', '16:9']

function AuraStudio() {
  const [active, setActive] = useState<'images' | 'videos'>('images')
  const [preset, setPreset] = useState(presets[0])
  const [ratio, setRatio] = useState('16:9')
  const [prompt, setPrompt] = useState('Un refugio brutalista suspendido sobre un acantilado, bañado por la luz ámbar del atardecer')
  const [generating, setGenerating] = useState(false)
  const [generated, setGenerated] = useState(true)
  const [videoGenerated, setVideoGenerated] = useState(false)
  const [videoProgress, setVideoProgress] = useState(0)
  const [mobileNav, setMobileNav] = useState(false)
  const [uploadedImage, setUploadedImage] = useState<string | null>(null)
  const [imageMode, setImageMode] = useState<'modify' | 'similar'>('modify')
  const fileInputRef = useRef<HTMLInputElement>(null)

  const handleUpload = (file: File) => {
    setUploadedImage(URL.createObjectURL(file))
    setImageMode('modify')
    toast.success('Imagen subida', { description: 'Describe los cambios o elige "Crear similar".' })
  }
  const optimizePrompt = () => { setPrompt(`${prompt.trim()}, iluminación de estudio cinematográfica, texturas hiperrealistas, composición editorial, detalle comercial premium, profundidad atmosférica`); toast.success('Prompt optimizado', { description: 'Añadimos iluminación, textura y dirección de arte.' }) }
  const generateImage = () => { setGenerating(true); setGenerated(false); window.setTimeout(() => { setGenerating(false); setGenerated(true); toast.success(uploadedImage ? (imageMode === 'modify' ? 'Imagen modificada' : 'Imagen similar creada') : 'Imagen lista para tu colección') }, 1400) }
  const generateVideo = () => { setVideoGenerated(false); setVideoProgress(12); const timer = window.setInterval(() => { setVideoProgress((value) => { if (value >= 100) { window.clearInterval(timer); setVideoGenerated(true); toast.success('Video renderizado en calidad Pro'); return 100 }; return value + 22 }) }, 380) }

  return (
    <div className="min-h-dvh bg-background text-foreground selection:bg-primary/30">
      <header className="flex h-16 items-center justify-between border-b border-border/70 px-5 lg:px-8">
        <div className="flex items-center gap-3"><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent hover:text-foreground lg:hidden" onClick={() => setMobileNav(!mobileNav)} aria-label="Abrir menú">{mobileNav ? <X className="size-5" /> : <Menu className="size-5" />}</button><div className="grid size-8 place-items-center rounded-xl border border-primary/40 bg-primary/10 text-primary"><Aperture className="size-4" /></div><span className="font-serif text-xl tracking-tight">AuraStudio <span className="text-primary">AI</span></span><span className="hidden rounded-full border border-border bg-muted/60 px-2 py-0.5 font-mono text-[9px] uppercase tracking-[0.18em] text-muted-foreground sm:inline">Pro workspace</span></div>
        <div className="flex items-center gap-3"><button className="hidden items-center gap-2 rounded-lg border border-border px-3 py-2 text-xs text-muted-foreground transition hover:border-primary/60 hover:text-foreground sm:flex"><Moon className="size-3.5" /> Dark mode</button><button className="grid size-9 place-items-center rounded-full bg-primary text-sm font-semibold text-primary-foreground shadow-[0_0_24px_-8px_var(--primary)]">AS</button></div>
      </header>
      <div className="mx-auto flex max-w-[1600px]">
        <aside className={`${mobileNav ? 'fixed inset-y-16 left-0 z-20 flex' : 'hidden'} w-64 shrink-0 flex-col border-r border-border/70 bg-sidebar p-4 lg:static lg:flex lg:min-h-[calc(100dvh-4rem)]`}>
          <div className="mb-6 flex items-center justify-between px-2 text-[10px] font-medium uppercase tracking-[0.2em] text-muted-foreground"><span>Workspace</span><MoreHorizontal className="size-4" /></div>
          <nav className="space-y-1"><NavItem active={active === 'images'} onClick={() => setActive('images')} icon={<ImageIcon className="size-4" />} label="Generador de imágenes" /><NavItem active={active === 'videos'} onClick={() => setActive('videos')} icon={<Film className="size-4" />} label="Generador de videos" /><button className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm text-muted-foreground transition hover:bg-accent/60 hover:text-foreground"><LayoutGrid className="size-4" /> Mi colección <span className="ml-auto rounded-md bg-muted px-1.5 py-0.5 font-mono text-[10px]">12</span></button></nav>
          <div className="mt-auto space-y-1 border-t border-border/70 pt-4"><button className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm text-muted-foreground transition hover:bg-accent/60 hover:text-foreground"><Settings2 className="size-4" /> Preferencias</button><div className="mt-4 rounded-2xl border border-primary/20 bg-primary/5 p-3"><div className="mb-2 flex items-center gap-2 text-xs font-medium"><Sparkles className="size-3.5 text-primary" /> Créditos Pro</div><div className="mb-2 flex items-end justify-between"><span className="font-mono text-[11px] text-muted-foreground">1.240 / 2.000</span><span className="text-[10px] text-primary">62%</span></div><div className="h-1 rounded-full bg-accent"><div className="h-full w-[62%] rounded-full bg-primary" /></div></div></div>
        </aside>
        <main className="min-w-0 flex-1 px-5 py-7 lg:px-10 lg:py-10"><div className="mx-auto max-w-6xl">
          <div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mb-2 font-mono text-[10px] uppercase tracking-[0.24em] text-primary">Create / {active === 'images' ? '01' : '02'}</p><h1 className="font-serif text-4xl tracking-tight sm:text-5xl">Hazlo visualmente<br /><em className="text-primary">extraordinario.</em></h1><p className="mt-3 max-w-lg text-sm leading-6 text-muted-foreground">Convierte una idea en una pieza visual con dirección de arte, textura y movimiento de nivel editorial.</p></div><div className="flex rounded-xl border border-border bg-card p-1"><button onClick={() => setActive('images')} className={`flex items-center gap-2 rounded-lg px-3 py-2 text-xs transition ${active === 'images' ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:text-foreground'}`}><ImageIcon className="size-3.5" /> Imágenes</button><button onClick={() => setActive('videos')} className={`flex items-center gap-2 rounded-lg px-3 py-2 text-xs transition ${active === 'videos' ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:text-foreground'}`}><Clapperboard className="size-3.5" /> Videos</button></div></div>
          <AnimatePresence mode="wait">{active === 'images' ? <motion.div key="images" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} transition={{ duration: 0.25 }} className="grid gap-6 xl:grid-cols-[380px_1fr]"><section className="space-y-5 rounded-2xl border border-border bg-card/70 p-5 shadow-lg"><FieldLabel text="Dirección de arte" /><div className="relative"><select value={preset} onChange={(event) => setPreset(event.target.value)} className="w-full appearance-none rounded-xl border border-border bg-background px-3 py-3 text-sm outline-none transition focus:border-primary"><option>{presets[0]}</option><option>{presets[1]}</option><option>{presets[2]}</option><option>{presets[3]}</option></select><ChevronDown className="pointer-events-none absolute right-3 top-3.5 size-4 text-muted-foreground" /></div><FieldLabel text="Relación de aspecto" /><div className="grid grid-cols-3 gap-2">{aspectRatios.map((item) => <button key={item} onClick={() => setRatio(item)} className={`rounded-xl border px-2 py-3 text-xs transition ${ratio === item ? 'border-primary bg-primary/10 text-primary' : 'border-border text-muted-foreground hover:border-primary/50'}`}>{item}<span className="mt-1 block text-[9px] opacity-60">{item === '9:16' ? 'Social' : item === '1:1' ? 'Square' : 'Landscape'}</span></button>)}</div>
          {/* Image upload section */}
          <div>
            <FieldLabel text="Imagen de referencia" />
            {uploadedImage ? (
              <div className="space-y-3">
                <div className="relative overflow-hidden rounded-xl border border-border">
                  <img src={uploadedImage} alt="Imagen subida" className="max-h-40 w-full object-cover" />
                  <button onClick={() => setUploadedImage(null)} className="absolute right-2 top-2 grid size-7 place-items-center rounded-lg border border-border/70 bg-background/80 text-foreground backdrop-blur transition hover:bg-primary hover:text-primary-foreground" aria-label="Quitar imagen"><X className="size-4" /></button>
                </div>
                <div className="grid grid-cols-2 gap-2">
                  <button onClick={() => setImageMode('modify')} className={`flex items-center justify-center gap-1.5 rounded-xl border px-2 py-2.5 text-xs transition ${imageMode === 'modify' ? 'border-primary bg-primary/10 text-primary' : 'border-border text-muted-foreground hover:border-primary/50'}`}><WandSparkles className="size-3.5" /> Modificar</button>
                  <button onClick={() => setImageMode('similar')} className={`flex items-center justify-center gap-1.5 rounded-xl border px-2 py-2.5 text-xs transition ${imageMode === 'similar' ? 'border-primary bg-primary/10 text-primary' : 'border-border text-muted-foreground hover:border-primary/50'}`}><Sparkles className="size-3.5" /> Crear similar</button>
                </div>
                {imageMode === 'modify' && <p className="text-[10px] leading-4 text-muted-foreground">Describe qué cambios quieres aplicar a la imagen.</p>}
                {imageMode === 'similar' && <p className="text-[10px] leading-4 text-muted-foreground">Describe el estilo o variación que quieres crear a partir de esta imagen.</p>}
              </div>
            ) : (
              <label className="flex cursor-pointer flex-col items-center justify-center gap-2 rounded-xl border border-dashed border-border p-6 text-muted-foreground transition hover:border-primary hover:text-primary">
                <ImageUp className="size-6" />
                <span className="text-center text-xs">Sube una imagen para modificarla<br />o crear algo similar</span>
                <input ref={fileInputRef} type="file" accept="image/*" className="hidden" onChange={(e) => { const file = e.target.files?.[0]; if (file) handleUpload(file) }} />
              </label>
            )}
          </div>
          <div><div className="mb-2 flex items-center justify-between"><FieldLabel text="Tu idea" /><span className="font-mono text-[10px] text-muted-foreground">{prompt.length}/500</span></div><textarea value={prompt} maxLength={500} onChange={(event) => setPrompt(event.target.value)} className="min-h-36 w-full resize-none rounded-xl border border-border bg-background p-3 text-sm leading-6 outline-none transition placeholder:text-muted-foreground/50 focus:border-primary" /></div><button onClick={optimizePrompt} className="flex w-full items-center justify-center gap-2 rounded-xl border border-primary/40 bg-primary/5 py-3 text-xs font-medium text-primary transition hover:bg-primary/15"><WandSparkles className="size-4" /> Optimizar con IA</button><button disabled={generating} onClick={generateImage} className="flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3.5 text-sm font-semibold text-primary-foreground shadow-[0_10px_30px_-15px_var(--primary)] transition hover:brightness-110 active:scale-[.98] disabled:cursor-wait disabled:opacity-70">{generating ? <><LoaderCircle className="size-4 animate-spin" /> Pintando tu visión...</> : <><Sparkles className="size-4" /> {uploadedImage ? (imageMode === 'modify' ? 'Modificar imagen' : 'Crear similar') : 'Generar imagen'} <span className="font-mono text-[10px] opacity-70">20 créditos</span></>}</button></section><PreviewCard generated={generated} ratio={ratio} uploadedImage={uploadedImage} imageMode={imageMode} /></motion.div> : <motion.div key="videos" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} transition={{ duration: 0.25 }} className="grid gap-6 xl:grid-cols-[380px_1fr]"><section className="space-y-5 rounded-2xl border border-border bg-card/70 p-5 shadow-lg"><FieldLabel text="Escena y movimiento" /><textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} className="min-h-44 w-full resize-none rounded-xl border border-border bg-background p-3 text-sm leading-6 outline-none transition focus:border-primary" placeholder="Un dolly-in lento sobre..." /><div className="grid grid-cols-2 gap-3"><label className="text-xs text-muted-foreground">Duración<select className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-foreground outline-none focus:border-primary"><option>5 segundos</option><option>10 segundos</option><option>15 segundos</option></select></label><label className="text-xs text-muted-foreground">Frame rate<select className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-foreground outline-none focus:border-primary"><option>24 fps</option><option>30 fps</option><option>60 fps</option></select></label></div><button onClick={generateVideo} disabled={videoProgress > 0 && videoProgress < 100} className="flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3.5 text-sm font-semibold text-primary-foreground transition hover:brightness-110 disabled:opacity-70">{videoProgress > 0 && videoProgress < 100 ? <><LoaderCircle className="size-4 animate-spin" /> Renderizando {videoProgress}%</> : <><Play className="size-4 fill-current" /> Generar video <span className="font-mono text-[10px] opacity-70">80 créditos</span></>}</button>{videoProgress > 0 && <div className="space-y-2"><div className="h-1.5 overflow-hidden rounded-full bg-accent"><div className="h-full rounded-full bg-primary transition-all duration-300" style={{ width: `${videoProgress}%` }} /></div><p className="text-[10px] text-muted-foreground">Analizando movimiento y renderizando fotogramas...</p></div>}</section><VideoPreview generated={videoGenerated} /></motion.div>}</AnimatePresence>
        </div></main>
      </div>
    </div>
  )
}

function NavItem({ active, onClick, icon, label }: { active: boolean; onClick: () => void; icon: React.ReactNode; label: string }) { return <button onClick={onClick} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm transition ${active ? 'bg-accent text-foreground shadow-sm' : 'text-muted-foreground hover:bg-accent/60 hover:text-foreground'}`}>{icon}{label}{active && <span className="ml-auto size-1.5 rounded-full bg-primary" />}</button> }
function FieldLabel({ text }: { text: string }) { return <label className="mb-2 block text-[10px] font-medium uppercase tracking-[0.18em] text-muted-foreground">{text}</label> }
function PreviewCard({ generated, ratio, uploadedImage, imageMode }: { generated: boolean; ratio: string; uploadedImage: string | null; imageMode: 'modify' | 'similar' }) { return <section className="min-w-0"><div className="mb-3 flex items-center justify-between"><div><span className="text-xs font-medium">Vista previa</span><span className="ml-2 font-mono text-[10px] text-muted-foreground">{ratio} · 2048 × 1152 px</span></div>{generated && <span className="flex items-center gap-1.5 text-[10px] text-primary"><span className="size-1.5 rounded-full bg-primary" /> Generado hace un momento</span>}</div><div className="group relative aspect-video overflow-hidden rounded-2xl border border-border bg-muted shadow-lg">{generated ? <><img src={imageUrl} alt="Refugio brutalista sobre un acantilado al atardecer" className="size-full object-cover transition duration-700 group-hover:scale-[1.02]" /><div className="absolute inset-0 bg-gradient-to-t from-background/70 via-transparent to-transparent" /><div className="absolute bottom-4 left-4 right-4 flex items-end justify-between"><div><p className="text-xs font-medium text-foreground">{uploadedImage ? (imageMode === 'modify' ? 'Imagen modificada' : 'Variación creada') : 'Golden hour / Brutalist retreat'}</p><p className="mt-1 font-mono text-[9px] uppercase tracking-[0.18em] text-foreground/60">AuraStudio render · {ratio}</p></div><a href={imageUrl} download target="_blank" rel="noreferrer" className="grid size-9 place-items-center rounded-lg border border-border/70 bg-background/70 text-foreground backdrop-blur transition hover:bg-primary hover:text-primary-foreground" aria-label="Descargar imagen"><ArrowDownToLine className="size-4" /></a></div></> : <div className="flex size-full flex-col items-center justify-center gap-3 text-muted-foreground"><LoaderCircle className="size-6 animate-spin text-primary" /><span className="text-xs">Creando composición...</span></div>}</div><div className="mt-4 grid grid-cols-3 gap-3"><div className="relative aspect-video overflow-hidden rounded-xl border border-primary/70"><img src={imageUrl} alt="Miniatura de imagen generada" className="size-full object-cover" /><span className="absolute bottom-2 left-2 rounded bg-background/80 px-1.5 py-0.5 font-mono text-[9px] text-primary">01</span></div>{uploadedImage ? <div className="relative aspect-video overflow-hidden rounded-xl border border-border opacity-80 transition hover:opacity-100"><img src={uploadedImage} alt="Imagen original subida" className="size-full object-cover" /><span className="absolute bottom-2 left-2 rounded bg-background/80 px-1.5 py-0.5 font-mono text-[9px] text-muted-foreground">{imageMode === 'modify' ? 'Original' : 'Referencia'}</span></div> : <div className="aspect-video overflow-hidden rounded-xl border border-border opacity-60 transition hover:opacity-100"><img src={secondImageUrl} alt="Referencia visual de paisaje" className="size-full object-cover" /></div>}<button className="flex aspect-video items-center justify-center rounded-xl border border-dashed border-border text-muted-foreground transition hover:border-primary hover:text-primary"><Plus className="size-4" /></button></div></section> }
function VideoPreview({ generated }: { generated: boolean }) { return <section className="min-w-0"><div className="mb-3 flex items-center justify-between"><div><span className="text-xs font-medium">Monitor de reproducción</span><span className="ml-2 font-mono text-[10px] text-muted-foreground">16:9 · 00:05 · 24 fps</span></div>{generated && <a href={sampleVideo} download target="_blank" rel="noreferrer" className="flex items-center gap-1.5 text-[10px] text-primary hover:underline"><ArrowDownToLine className="size-3" /> Descargar MP4</a>}</div><div className="group relative aspect-video overflow-hidden rounded-2xl border border-border bg-muted shadow-lg">{generated ? <><video src={sampleVideo} controls loop className="size-full object-cover" poster={imageUrl} /><div className="pointer-events-none absolute left-4 top-4 rounded-md border border-border/70 bg-background/70 px-2 py-1 font-mono text-[9px] text-primary backdrop-blur">AURASTUDIO / MOTION 01</div></> : <div className="flex size-full flex-col items-center justify-center gap-3 text-muted-foreground"><div className="grid size-14 place-items-center rounded-full border border-primary/40 bg-primary/10 text-primary"><Play className="ml-1 size-6 fill-current" /></div><span className="text-xs">Tu preview aparecerá aquí</span></div>}</div><div className="mt-4 flex items-center gap-3 rounded-xl border border-border bg-card/60 p-3 text-xs text-muted-foreground"><Clock3 className="size-4 text-primary" />La generación de movimiento puede tardar unos segundos.</div></section> }
